//
//  ContentView.swift
//  TestAFE
//
//  Created by dimitri on 29/01/2024.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, blablalblalbala!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
